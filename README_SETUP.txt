Архив содержит заготовку директории app до пункта 10.

Ожидается, что в корне проекта уже будут:
- config.py с классом Config
- templates/
- static/

Отдельный run.py потом может импортировать create_app из app.
